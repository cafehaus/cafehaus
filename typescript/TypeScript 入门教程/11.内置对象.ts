// ECMAScript 的内置对象
// Boolean、Error、Date、RegExp...
let b: Boolean = new Boolean(1)
let e: Error = new Error('fail')
let d: Date = new Date()
let r: RegExp = /a-zA-Z/

// DOM 和 BOM 的内置对象
// Document、HTMLElement、Event、NodeList...
let body: HTMLElement = document.body
let divs: NodeList = document.querySelectorAll('div')